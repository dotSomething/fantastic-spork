const SCRIPTS = require('./scripts');
const DATA = require('./data');

SCRIPTS.reset(DATA.SPACECRAFT_DATA.Intcodes);
// console.log(SCRIPTS.calculateFuel(DATA.SPACECRAFT_DATA['Module Masses']));
